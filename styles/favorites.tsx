import { StyleSheet } from "react-native";

const favorites = StyleSheet.create({
    image: {
        width: 100,
        height: 100,
        margin: 1,
    },
    container: {
        flex: 1,
    }
})

export default favorites;