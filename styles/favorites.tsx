import { StyleSheet } from "react-native";

const favorites = StyleSheet.create({
    image: {
        width: 100,
        height: 100
    },
    grid: {
        flex: 1,
        flexDirection: "row",
    }
})

export default favorites;